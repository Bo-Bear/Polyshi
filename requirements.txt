# Core dependencies (always needed)
requests>=2.28
python-dotenv>=1.0

# WebSocket for real-time Polymarket orderbook (recommended)
websocket-client>=1.6

# Live trading dependencies (only needed when EXEC_MODE=live)
cryptography>=41.0          # Kalshi RSA-PSS auth signing
py-clob-client>=0.15        # Polymarket CLOB order placement
web3==6.14.0                # Pinned — py-clob-client dependency (avoids eth-typing conflicts)
