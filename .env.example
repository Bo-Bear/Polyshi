# ============================================================
# Polyshi Configuration
# Copy this to .env and fill in your values
# ============================================================

# --- Execution Mode ---
# "paper" = simulated trades (default), "live" = real money
EXEC_MODE=paper

# --- Trade Parameters ---
PAPER_CONTRACTS=10          # Number of contracts per trade
MAX_TEST_TRADES=5           # Stop after this many trades
SCAN_SLEEP_SECONDS=5        # Seconds between scan cycles

# --- Execution Safeguards ---
MIN_NET_EDGE=0.03           # Minimum net edge to accept (3%)
MAX_TOTAL_COST=0.98         # Max gross cost (room for slippage)
MIN_WINDOW_REMAINING_S=30   # Min seconds left in window
MAX_SPREAD=0.10             # Max bid-ask spread
PRICE_FLOOR=0.10            # Skip prices below this
PRICE_CEILING=0.98          # Skip prices above this
MAX_PROB_DIVERGENCE=0.15    # Max P(up) divergence between exchanges

# --- Order Execution (live mode) ---
ORDER_TIMEOUT_S=15          # Max seconds to wait for order fill
ORDER_POLL_INTERVAL_S=1     # How often to poll order status
POLY_FILL_MAX_RETRIES=5     # Poly orderbook retry attempts (after Kalshi fills first)
POLY_FILL_RETRY_TIMEOUT_S=3 # Seconds per Poly retry attempt

# --- Per-Coin Limits ---
MAX_TRADES_PER_COIN=5       # Max trades per coin per session (incl. unwinds)
MAX_CONSECUTIVE_UNWINDS=3   # Stop coin after N consecutive unwinds

# --- Kalshi Series Tickers (override if Kalshi renames them) ---
# KALSHI_SERIES_BTC=KXBTC15M
# KALSHI_SERIES_ETH=KXETH15M
# KALSHI_SERIES_SOL=KXSOL15M
# KALSHI_SERIES_XRP=KXXRP15M

# ============================================================
# KALSHI CREDENTIALS (required for EXEC_MODE=live)
# ============================================================
# 1. Go to https://kalshi.com/account/profile -> API Keys
# 2. Click "Create New API Key"
# 3. Save the Key ID and download the RSA private key (.pem file)

KALSHI_API_KEY_ID=           # Your Kalshi API key ID
KALSHI_PRIVATE_KEY_PATH=     # Absolute path to your RSA private key .pem file

# ============================================================
# POLYMARKET CREDENTIALS (required for EXEC_MODE=live)
# ============================================================
# You need an Ethereum/Polygon wallet with USDC.e funded
# The private key is used to sign orders on the CLOB

POLY_PRIVATE_KEY=            # Your Ethereum private key (hex, starting with 0x)
POLY_SIGNATURE_TYPE=0        # 0=EOA (MetaMask/hardware), 1=Magic/email wallet, 2=browser/Gnosis Safe
POLY_FUNDER_ADDRESS=         # Required ONLY for signature_type 1 or 2 (leave blank for type 0)
